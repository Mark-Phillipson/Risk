Place the social preview image here as `world-conquest-social.png` (recommended size 1200x630 px).

The app's `index.html` references this path for Open Graph and Twitter Card previews:

- `/img/world-conquest-social.png`

If you prefer a different filename, update the `og:image` and `twitter:image` meta tags in `wwwroot/index.html` to match.
